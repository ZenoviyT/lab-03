# CMPUT 301 : Lab 3 Participation Exercise

## Student Details

- **Full Name:** Zenoviy Talpash
- **CCID:** zenoviy

## References and Resources
- https://www.baeldung.com/kotlin/underscore-type-arguments : This was used to fix the CityListScreenPreview() function, since I wasn't sure how to have multiple unused lambdas, unlike ```onAddCity = {}```. This doesn't even do anything in code, this is just for the Incorrect Formatting Inspection to not give me errors.

- Android Studio "Show Quick-Fixes" Command : This came about because I forgot to check if a variable is null when sending its data to another function. The IDE displayed an error, and after trying to find what line it was on, I found the "Show Quick Fixes" command, which recommended adding a non-null assertion call (which is just adding ```!!``` at the end of a variable). I didn't know this was a thing, but it was more convenient than writing ```if ([variable] != null)```. Still, this did help beyond the realm of my own knowledge, so I feel that its worthy to note it down.

- The Lab 3 Instructions : I did end up using the ```updateCity()``` function from the notes (besides the ```CityListScreen.kt``` edits), so I'm noting this down just to be safe.

## Verbal Collaboration
N/A
